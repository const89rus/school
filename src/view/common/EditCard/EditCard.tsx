import React from 'react';

/*import * as S from './styled';

interface Props {
  name: string;
  img: string;
  position: string;
}

export const EditCard: React.FC<Props> = ({ name, img, position }) => {
  return (
    <S.EditCardWrapper>
      <S.Input type="file" defaultValue={img}></S.Input>
      <S.Input type="text" placeholder="ФИО" defaultValue={name}></S.Input>
      <S.Input type="text" placeholder="Описание" defaultValue={position}></S.Input>
    </S.EditCardWrapper>
  );
};*/
